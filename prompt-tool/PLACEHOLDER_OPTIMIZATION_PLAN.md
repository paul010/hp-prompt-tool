# 提示词占位符优化方案

## 多语言支持 🌐

### 支持语言
- 英文 (en) - **默认语言**
- 简体中文 (zh-CN)
- 繁体中文 (zh-TW) / 台灣
- 日本語 (ja)
- 한국어 (ko)

### 多语言架构

#### 语言切换状态
```typescript
// App Context/State
interface AppSettings {
  language: "en" | "zh-CN" | "zh-TW" | "ja" | "ko";
  autoDetect: boolean;  // 自动检测浏览器语言
}

// 本地存储
localStorage.setItem("prompt-language", "zh-CN");
```

#### 多语言提示词结构
```typescript
interface LocalizedContent {
  en: string;      // 英文（必填，作为默认值）
  "zh-CN"?: string;
  "zh-TW"?: string;
  ja?: string;
  ko?: string;
}

interface Prompt {
  id: string;
  name: LocalizedContent;        // 多语言名称
  nameZh?: string;               // @deprecated 兼容旧版
  description: LocalizedContent; // 多语言描述
  content: LocalizedContent;     // 多语言提示词内容
  // ... 其他字段

  // inputFields 也支持多语言
  inputFields?: LocalizedInputField[];
}

interface LocalizedInputField extends Omit<InputField, "label" | "placeholder" | "hint"> {
  label: LocalizedContent;
  placeholder?: LocalizedContent;
  hint?: LocalizedContent;
}
```

#### 语言降级策略
```
用户选择: zh-CN
├── content["zh-CN"] 存在 → 使用简体中文
├── content["zh-CN"] 不存在
│   ├── content["zh-TW"] 存在 → 使用繁体中文
│   ├── content["ja"] 存在 → 使用日语
│   └── content["en"] 存在 → 使用英文（兜底）
```

#### UI 显示示例

```typescript
// 根据当前语言获取内容
function getLocalized<T extends LocalizedContent>(
  localized: T,
  language: string
): string {
  return localized[language] || localized["zh-TW"] || localized["ja"] || localized["ko"] || localized["en"];
}

// 使用
const currentLanguage = "zh-CN";
const displayName = getLocalized(prompt.name, currentLanguage);
const displayContent = getLocalized(prompt.content, currentLanguage);
```

#### 语言选择器组件

```typescript
// components/LanguageSelector.tsx
const LANGUAGES = [
  { code: "en", name: "English", flag: "🇺🇸" },
  { code: "zh-CN", name: "简体中文", flag: "🇨🇳" },
  { code: "zh-TW", name: "繁體中文", flag: "🇹🇼" },
  { code: "ja", name: "日本語", flag: "🇯🇵" },
  { code: "ko", name: "한국어", flag: "🇰🇷" },
];

// 在 Header 中显示语言切换器
<select value={language} onChange={handleLanguageChange}>
  {LANGUAGES.map(lang => (
    <option value={lang.code}>{lang.flag} {lang.name}</option>
  ))}
</select>
```

---

## 当前系统分析

---

## 当前系统分析

### 现有格式
```typescript
// 简单占位符
content: "Write a cold email to a [job title] at [company name]..."

// inputFields 只是字符串数组
inputFields: ["job title", "company name", "value props"]
```

### 存在的问题
1. ❌ 无法区分必填/可选字段
2. ❌ 缺少字段类型提示（文本、数字、日期、列表等）
3. ❌ 没有默认值或示例
4. ❌ 多行输入没有明确标识
5. ❌ 用户需要手动编辑复制的文本
6. ❌ 缺少字段验证提示
7. ❌ 没有字段分组或顺序控制

---

## 优化方案设计

### Phase 1: 增强类型系统

#### 新增 InputField 接口
```typescript
interface InputField {
  name: string;           // 字段标识符（用于占位符）
  label: string;          // 显示名称（中文）
  labelEn?: string;       // 英文标签
  type: FieldType;        // 字段类型
  required: boolean;      // 是否必填
  placeholder?: string;   // 输入提示
  defaultValue?: string;  // 默认值
  options?: string[];     // 选项（用于 select 类型）
  min?: number;          // 最小值/长度
  max?: number;          // 最大值/长度
  multiline?: boolean;   // 是否多行输入
  rows?: number;         // 多行行数
  group?: string;        // 分组名称
  order?: number;        // 显示顺序
  hint?: string;         // 填写提示
}

type FieldType =
  | "text"        // 短文本
  | "textarea"    // 长文本/多行
  | "number"      // 数字
  | "date"        // 日期
  | "select"      // 单选
  | "multiselect" // 多选
  | "email"       // 邮箱
  | "url"         // URL
  | "json";       // JSON 数据
```

#### 更新 Prompt 接口
```typescript
interface Prompt {
  // ... 现有字段
  inputFields?: InputField[];  // 从 string[] 改为 InputField[]
}
```

---

### Phase 2: 占位符格式规范

#### 基础格式（向后兼容）
```
[field_name]
```

#### 扩展格式（带元数据）
```
[field_name|type|required|default_value]
```

**示例：**
```
写一封给 [recipient_email|email|required] 的邮件，
主题是 [subject|text|required|关于项目更新]，
内容是 [body|textarea|required]，
抄送给 [cc_list|email|optional]，
优先级为 [priority|select|required|高,中,低|中]
```

---

### Phase 3: UI/UX 增强

#### 3.1 新增 PromptBuilderModal 组件
- 自动解析 inputFields 生成表单
- 实时预览填充后的提示词
- 一键复制已填充的提示词

#### 3.2 占位符高亮显示
- 在 PromptCard 中高亮显示占位符
- 区分必填/可选字段

#### 3.3 快速填充功能
- 保存常用字段值
- 历史记录

---

## 实施步骤

### Step 1: 类型定义更新

- [ ] 在 `types.ts` 中添加多语言和 InputField 相关类型
- [ ] 添加 `LocalizedContent` 类型
- [ ] 添加 `InputField` 和 `LocalizedInputField` 接口
- [ ] 更新 `Prompt` 接口支持多语言
- [ ] 添加 `Language` 类型定义

### Step 2: 多语言基础设施

- [ ] 创建 `lib/i18n.ts` 国际化工具
  - `getLocalized(content, lang)` - 获取本地化内容
  - `getDefaultLanguage()` - 获取默认语言
  - `saveLanguagePreference(lang)` - 保存语言偏好
- [ ] 创建 `contexts/LanguageContext.tsx` 语言上下文
- [ ] 创建 `components/LanguageSelector.tsx` 语言选择器组件
- [ ] 在 `layout.tsx` 中集成语言提供者

### Step 3: 数据迁移

- [ ] 更新 `openaiPrompts.ts` 数据结构
  - 将 `name`/`nameZh` 转换为 `name: { en, zh-CN, zh-TW, ja, ko }`
  - 将 `description` 转换为多语言格式
  - 将 `content` 转换为多语言格式
  - 将 `inputFields` 从 `string[]` 转换为 `InputField[]`
- [ ] 为所有 70 个 OpenAI 提示词添加多语言翻译
- [ ] 为字段添加元数据（类型、必填、分组等）

### Step 4: 占位符解析工具

- [ ] 创建 `lib/placeholders.ts` 工具函数
  - `extractPlaceholders(content)` - 从内容中提取占位符
  - `fillPlaceholders(content, values)` - 填充占位符
  - `validatePlaceholders(content, values)` - 验证必填字段
  - `detectPlaceholderPattern(content)` - 检测占位符格式

### Step 5: UI 组件开发

- [ ] 创建 `PromptBuilderModal.tsx` - 提示词构建器
  - 表单式填写占位符
  - 实时预览填充结果
  - 语言切换时同步更新
- [ ] 创建 `PlaceholderHighlight.tsx` - 占位符高亮组件
  - 区分必填/可选字段样式
  - 多语言占位符显示
- [ ] 更新 `PromptCard.tsx`
  - 集成语言切换功能
  - 显示多语言内容
  - 集成 PromptBuilderModal
- [ ] 更新 `PromptDetailModal.tsx`
  - 支持多语言显示
  - 添加"构建提示词"按钮

### Step 6: 翻译管理

- [ ] 创建 `translations/` 目录存放翻译文件
- [ ] 为 UI 文本添加翻译（简体中文、繁体中文、日语、韩语）
- [ ] 建立翻译工作流程和规范

### Step 7: 测试和优化

- [ ] 测试各字段类型的渲染和验证
- [ ] 测试语言切换功能
- [ ] 优化移动端体验
- [ ] 添加字段值的本地存储（按语言分别存储）
- [ ] 性能优化（懒加载翻译、缓存等）

---

## 数据示例

### Before (单语言)

```typescript
{
  id: "sales-cold-email",
  name: "Cold Outreach Email",
  nameZh: "个性化开发邮件",
  description: "为特定职位和公司撰写有针对性的开发邮件",
  content: "Write a cold email to a [job title] at [company name]...",
  inputFields: ["job title", "company name", "value props"]
}
```

### After (多语言 + InputField 对象)

```typescript
{
  id: "sales-cold-email",
  name: {
    en: "Cold Outreach Email",
    "zh-CN": "个性化开发邮件",
    "zh-TW": "个性化開發郵件",
    ja: "コールド outreachメール",
    ko: "콜드 아웃리치 이메일"
  },
  description: {
    en: "Create personalized cold outreach emails for specific roles and companies",
    "zh-CN": "为特定职位和公司撰写有针对性的开发邮件",
    "zh-TW": "為特定職位和公司撰寫有針對性的開發郵件",
    ja: "特定の役割と会社向けのパーソナライズされたコールドメールを作成",
    ko: "특정 직무와 회사를 위한 개인화된 콜드 이메일 작성"
  },
  content: {
    en: "Write a short, compelling cold email to a [job title] at [company name] introducing our product.\n\nBackground: [value props]\n\nFormat it in email-ready text with a clear subject line.",
    "zh-CN": "给 [company name] 的 [job title] 写一封简短而有说服力的开发邮件，介绍我们的产品。\n\n背景信息：[value props]\n\n格式为邮件就绪文本，包含清晰的主题行。",
    "zh-TW": "給 [company name] 的 [job title] 寫一封簡短而有說服力的開發郵件，介紹我們的產品。\n\n背景資訊：[value props]\n\n格式為郵件就緒文字，包含清晰的主旨行。",
    ja: "[company name]の[job title]向けに、当社の製品を紹介する簡潔で説得力のあるコールドメールを作成してください。\n\n背景: [value props]\n\nメール形式で、明確な件名を含めてください。",
    ko: "[company name]의 [job title]에게 당사 제품을 소개하는 간결하고 설득력 있는 콜드 이메일을 작성하세요.\n\n배경: [value props]\n\n이메일 형식으로 명확한 제목을 포함하세요."
  },
  inputFields: [
    {
      name: "job title",
      label: {
        en: "Job Title",
        "zh-CN": "职位名称",
        "zh-TW": "職位名稱",
        ja: "役職",
        ko: "직함"
      },
      type: "text",
      required: true,
      placeholder: {
        en: "e.g., CTO, VP of Marketing",
        "zh-CN": "例如: CTO, 市场副总裁",
        "zh-TW": "例如: CTO, 行銷副總裁",
        ja: "例: CTO、マーケティング担当VP",
        ko: "예: CTO, 마케팅 VP"
      },
      group: "basic_info",
      order: 1,
      hint: {
        en: "Recipient's job title",
        "zh-CN": "收件人的职位",
        "zh-TW": "收件人的職位",
        ja: "受信者の役職",
        ko: "수신자의 직함"
      }
    },
    {
      name: "company name",
      label: {
        en: "Company Name",
        "zh-CN": "公司名称",
        "zh-TW": "公司名稱",
        ja: "会社名",
        ko: "회사명"
      },
      type: "text",
      required: true,
      placeholder: {
        en: "e.g., Acme Corp",
        "zh-CN": "例如: 某某科技有限公司",
        "zh-TW": "例如: 某某科技有限公司",
        ja: "例: ○○株式会社",
        ko: "예: 00주식회사"
      },
      group: "basic_info",
      order: 2
    },
    {
      name: "value props",
      label: {
        en: "Value Propositions",
        "zh-CN": "产品价值主张",
        "zh-TW": "產品價值主張",
        ja: "価値提案",
        ko: "가치 제안"
      },
      type: "textarea",
      required: true,
      placeholder: {
        en: "Describe the core value of your product...",
        "zh-CN": "描述产品的核心价值...",
        "zh-TW": "描述產品的核心價值...",
        ja: "製品の中核価値を説明...",
        ko: "제품의 핵심 가치 설명..."
      },
      rows: 4,
      group: "product_info",
      order: 3,
      hint: {
        en: "List 3-5 key value points",
        "zh-CN": "列出3-5个核心价值点",
        "zh-TW": "列出3-5個核心價值點",
        ja: "3〜5つの主要な価値ポイントをリストアップ",
        ko: "3-5개 핵심 가치 포인트 나열"
      }
    }
  ]
}
```

---

## 预期效果

### 占位符系统优化
1. ✅ 用户通过表单填写字段，无需手动编辑
2. ✅ 清晰的字段类型和验证提示
3. ✅ 必填/可选字段一目了然
4. ✅ 支持默认值和示例
5. ✅ 更好的用户体验和填写效率

### 多语言支持
1. ✅ 支持 5 种语言切换（英、简中、繁中、日、韩）
2. ✅ 提示词内容、字段标签、提示文本全部本地化
3. ✅ 智能语言降级（缺失翻译时自动回退）
4. ✅ 语言偏好本地存储
5. ✅ 默认英文，面向全球用户

---

## 技术架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户界面层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │LanguageSelect│  │ PromptCard   │  │PromptBuilder │      │
│  │    语言选择   │  │  提示词卡片   │  │  提示词构建器 │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        状态管理层                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              LanguageContext (语言上下文)              │   │
│  │  - currentLanguage: "en" | "zh-CN" | "zh-TW" | "ja" | "ko" │   │
│  │  - setLanguage(lang)                                │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        工具函数层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  i18n.ts     │  │ placeholders │  │  validation  │      │
│  │  国际化工具   │  │  占位符工具   │  │  验证工具     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        数据层                                │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                    Prompt 数据结构                     │   │
│  │  name: { en, zh-CN, zh-TW, ja, ko }                  │   │
│  │  content: { en, zh-CN, zh-TW, ja, ko }               │   │
│  │  inputFields: LocalizedInputField[]                  │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 文件结构

```
prompt-tool/
├── src/
│   ├── lib/
│   │   ├── types.ts              # 类型定义（新增 LocalizedContent, InputField）
│   │   ├── i18n.ts               # 国际化工具（新建）
│   │   ├── placeholders.ts       # 占位符工具（新建）
│   │   └── platforms.ts
│   ├── contexts/
│   │   └── LanguageContext.tsx   # 语言上下文（新建）
│   ├── components/
│   │   ├── PromptCard.tsx        # 更新：支持多语言
│   │   ├── PromptDetailModal.tsx # 更新：支持多语言
│   │   ├── LanguageSelector.tsx  # 语言选择器（新建）
│   │   ├── PromptBuilderModal.tsx # 提示词构建器（新建）
│   │   └── PlaceholderHighlight.tsx # 占位符高亮（新建）
│   ├── data/
│   │   └── openaiPrompts.ts      # 更新：多语言 + InputField
│   └── translations/             # 翻译文件（新建）
│       ├── en.json
│       ├── zh-CN.json
│       ├── zh-TW.json
│       ├── ja.json
│       └── ko.json
└── PLACEHOLDER_OPTIMIZATION_PLAN.md
```
