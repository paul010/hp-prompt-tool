name: Sync Prompts Data

on:
  schedule:
    # 每小时运行一次 (UTC 时间)
    - cron: '0 * * * *'
  workflow_dispatch: # 允许手动触发

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install
        working-directory: ./prompt-tool

      - name: Sync prompts from upstream (CSV)
        run: node scripts/sync-prompts.js

      - name: Sync image prompts from prompts.chat
        run: node scripts/fetch-prompts-chat-images.js

      - name: Check for changes
        id: git-check
        run: |
          git diff --quiet prompt-tool/data/prompts.csv prompt-tool/src/data/promptsChatImagePrompts.ts || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add prompt-tool/data/prompts.csv prompt-tool/src/data/promptsChatImagePrompts.ts
          git commit -m "chore: sync prompts data [skip ci]"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        run: |
          if [ "${{ steps.git-check.outputs.changed }}" == "true" ]; then
            echo "✅ Data updated and pushed to repository"
          else
            echo "ℹ️ No new data to sync"
          fi
